server:
  port: 8000
spring:
  application:
    name: HairStyle
  datasource:
    url: jdbc:postgresql://localhost:5432/HairStyle?serverTimeZone=ADT
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
spring-doc:
  api-docs:
    path: /api-docs
    pathsToMatch: /api/**
    swagger-ui:
      use-root-path: true
